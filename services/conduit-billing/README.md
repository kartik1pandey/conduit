# conduit-billing

Meters API calls per merchant and computes a tiered invoice on a schedule.

Lower priority — scaffolded in Phase 0, implementation begins Phase 4 (cut first
if time runs short) — see `docs/CHECKPOINTS.md`.
